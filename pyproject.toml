[project]
name = "career-ops-cv"
version = "0.1.0"
description = "CV rendering via WeasyPrint (used by the tailor mode and the dashboard)"
requires-python = ">=3.11"
dependencies = [
    "weasyprint>=62",
    "markdown>=3.6",
    "pypdf>=4",
]

[tool.uv]
package = false
