# Career-Ops Profile Configuration
# Copy this file to config/profile.yml and fill in your details.
# This is the single source of truth for your personal data across all modes.

candidate:
  full_name: "Jane Smith"
  email: "jane@example.com"
  phone: "+1-555-0123"
  location: "San Francisco, CA"
  linkedin: "linkedin.com/in/janesmith"
  portfolio_url: "https://janesmith.dev"
  github: "github.com/janesmith"
  twitter: "https://x.com/janesmith"
  # (Optional) Canva resume design ID for visual CV generation via /career-ops pdf.
  # Find it in your Canva design URL: https://www.canva.com/design/DAxxxxxxx/...
  # The ID starts with "D" and is 11 characters long.
  # canva_resume_design_id: "DAxxxxxxxxx"

target_roles:
  # Your North Star roles — what you're optimizing for
  primary:
    - "Senior AI Engineer"
    - "Staff ML Engineer"
  # Archetypes help the evaluation system score fit
  archetypes:
    - name: "AI/ML Engineer"
      level: "Senior/Staff"
      fit: "primary"        # primary = dream role, secondary = good fit, adjacent = stretch
    - name: "AI Product Manager"
      level: "Senior"
      fit: "secondary"
    - name: "Solutions Architect"
      level: "Mid-Senior"
      fit: "adjacent"

narrative:
  # Your professional headline (1 line)
  headline: "ML Engineer turned AI product builder"
  # Your exit story — what makes you unique
  exit_story: "Built and sold my SaaS after 5 years. Now focused on applied AI at scale."
  # Your top 3-5 superpowers
  superpowers:
    - "End-to-end ML pipelines"
    - "Fast prototyping (idea to prod in 2 weeks)"
    - "Cross-functional communication"
  # Proof points — projects, articles, case studies with measurable impact
  proof_points:
    - name: "Project Alpha"
      url: "https://janesmith.dev/project-alpha"
      hero_metric: "Reduced inference latency 40%"
    - name: "Open Source Tool"
      url: "https://github.com/janesmith/tool"
      hero_metric: "2K+ GitHub stars"
  # Optional: dashboard/demo URL with credentials
  # dashboard:
  #   url: "https://janesmith.dev/demo"
  #   password: "demo-2026"

compensation:
  target_range: "$150K-200K"     # Your target total comp
  currency: "USD"
  minimum: "$120K"               # Walk-away number
  location_flexibility: "Remote preferred, 1 week/month on-site possible"

location:
  country: "United States"
  city: "San Francisco"
  timezone: "PST"
  visa_status: "No sponsorship needed"
  # For remote roles outside your country:
  # onsite_availability: "1 week/month in any city"

# Optional — read by modes/_location-gate.md to auto-skip roles you can't take.
# Remove or comment out skip_on entries you don't want enforced. Delete the
# whole block to disable the gate (everything will be scored).
location_policy:
  home_country: "United States"
  home_timezone: "PST"
  # Allowed clock offset from home_timezone. e.g. tolerance 3 from PST allows
  # roles explicitly demanding MST/CST. Roles that name a timezone outside
  # that band (e.g. "must overlap 9-5 CET") are skipped.
  timezone_tolerance_hours: 3
  # Do you already have work authorization for the US? If false and a role
  # requires US auth ("H1B only", "US citizens only"), it is skipped.
  us_work_authorization: true
  # Would you relocate for the right role? If false and a role requires on-site
  # with relocation, it is skipped.
  relocation_open: false
  # Remote scope labels that are OK for you. If a posting says "Remote — US"
  # and "US" is in this list, the role passes the remote-scope rule.
  remote_allowed_scopes:
    - "Global"
    - "Worldwide"
    - "Americas"
    - "US"
    - "United States"
  # Which rules to enforce. Comment out or remove to disable individual rules.
  skip_on:
    - remote_scope_excludes_home_country
    - onsite_outside_home_country
    - us_work_auth_required
    - onsite_and_relocation_required
    - timezone_outside_home_tolerance
